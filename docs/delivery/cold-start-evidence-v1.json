{
  "evidence_version": "cold-start-v1",
  "environment": "clean checkout (machine path omitted)",
  "python_locked_install": "passed",
  "python_pytest": "53 passed, 1 skipped (live Redis test is opt-in)",
  "frontend_clean_npm_ci": "passed",
  "frontend_clean_check": "passed",
  "frontend_clean_build": "passed",
  "live_redis_celery_recovery_test": "covered by the opt-in integration test and CI job; external runtime evidence must be collected by the operator",
  "compose_health_smoke": "reproducible through the documented Compose smoke flow",
  "docker_compose_start": "docker compose up --build -d",
  "production_cold_start": false,
  "limitations": [
    "This file records PoC validation scope only; it is not a production certification.",
    "A clean external environment should rerun the Compose and live-worker checks before production use."
  ]
}
