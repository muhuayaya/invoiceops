## 1. 仓储与 API 读取修复

- [x] 1.1 为 PostgreSQL 仓储增加完整快照刷新能力，并确保刷新不会重复累积审计、复核和 Outbox 数据；通过仓储重载测试验证刷新后的集合数量和内容正确
- [x] 1.2 在 API 的工单列表、人工复核、复核历史、审计、指标以及依赖已有工单的操作入口调用刷新能力；通过 API 集成测试验证 worker 写入后无需重启即可读取

## 2. 回归验证

- [x] 2.1 增加两个共享数据库仓储实例的跨进程写入/读取测试，覆盖管理员数据列表、复核队列、审计事件和指标；执行目标测试并确认通过
- [x] 2.2 执行完整 Python 测试、前端类型检查、前端构建和 OpenSpec 严格校验，确认没有回归

## 3. 虚拟机部署验证

- [x] 3.1 将变更同步到 `192.168.88.100:/home/itheima/invoiceops` 并重建 API/worker；确认 Compose 服务健康且批次完成后各读模块无需重启即可展示数据
