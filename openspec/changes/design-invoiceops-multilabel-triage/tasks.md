## 1. M0：仓库与共享契约

- [x] 1.1 [B] 创建约定的 Python、React、ML、测试、配置和基础设施目录，补充 `.gitignore`，并以目录清单和敏感制品未被 Git 跟踪作为验证
- [x] 1.2 [B] 配置 `pyproject.toml`、Python 锁定依赖、前端依赖和统一开发命令，并验证全新环境能够完成依赖安装
- [x] 1.3 [A] 编写 `invoiceops-v1` 标签、风险等级、示例、路由和阈值 JSON Schema，使用正反例校验测试验证未知标签和非法阈值会失败
- [x] 1.4 [B] 定义 OpenAPI 草案、标准错误响应、幂等头和批次 CSV 模板，并用契约测试验证 schema 可解析且示例请求有效
- [x] 1.5 [ALL] 评审并冻结标签 v1、API v1、事件格式和 PoC 指标，将评审结论记录为 ADR，并验证三名负责人均有明确所有权

## 2. 数据与评估基线

- [x] 2.1 [A] 建立公开语料与模拟数据来源清单，记录许可、用途和禁用范围，并验证每个输入文件都有来源和校验值
- [x] 2.2 [A] 建立双语多标签场景矩阵与生成模板，覆盖 8 类标签、常见多标签组合、否定句和中英混合文本，并用覆盖报告验证每个目标组合都有样本
- [x] 2.3 [A] 实现数据规范化、语言识别、规则脱敏和白名单元数据处理，并用邮箱、手机号、IBAN/银行账号、发票号和混合语言单元测试验证
- [x] 2.4 [A] 实现近重复/模板组检测和多标签迭代分层切分，验证模板、翻译对、近重复文本和模拟供应商上下文不会跨 train/dev/test 泄漏
- [x] 2.5 [A+C] 人工审核训练候选并建立双人复核的冻结挑战集，输出数据卡，验证标签分布、语言分布、多标签比例、重复率和已知偏差均有记录
- [x] 2.6 [A] 实现 TF-IDF + One-vs-Rest Logistic Regression 基线及统一评估器，验证报告包含 micro/macro-F1、逐标签指标、Hamming loss、语言分组、时延和模型大小

## 3. 主要模型与模型生命周期

- [x] 3.1 [A] 实现 XLM-R 多标签数据加载器、sigmoid 分类头和可复现训练配置，并用固定种子重复运行小样本测试验证输出一致性
- [x] 3.2 [A] 完成首轮 XLM-R 训练和误差分析，输出训练记录、混淆样例与模型卡，并验证所有制品引用同一冻结数据集版本
- [x] 3.3 [A] 实现逐标签候选阈值、自动接受阈值和置信度校准，验证低置信度、无标签和冲突组合均产生预期拒识原因码
- [x] 3.4 [A] 实现统一模型运行时和版本化制品装载接口，验证轻量基线与 XLM-R 可通过同一推理契约替换
- [x] 3.5 [A+B] 实现模型注册、准入、active alias 和上一稳定版本回滚，验证关键类召回不达标时无法提升且回滚后新请求使用旧版本
- [x] 3.6 [A] 在约定硬件执行冻结评估与至少 500 次性能测试，验证模型指标和 P95 门槛并记录完整测试环境；若未达标则记录阻塞而不伪造通过

## 4. 后端领域与持久化

- [x] 4.1 [B] 实现标签、工单、预测、复核、模型版本和审计的领域对象及状态转换，并用单元测试验证非法状态转换被拒绝
- [x] 4.2 [B] 建立 PostgreSQL schema 和可重复迁移，覆盖核心业务表、唯一键、外键与只追加审计约束，并在空库和已有库各执行一次迁移测试
- [x] 4.3 [B] 实现单条工单接入、字段校验、正文限制、白名单元数据和 `trace_id`，用有效、中英混合、空文本和超长文本 API 测试验证
- [x] 4.4 [B] 实现幂等键存储与冲突检测，验证同键同体返回首次结果、同键异体返回冲突且数据库无重复记录
- [x] 4.5 [A+B] 接入模型运行时、逐标签阈值、高风险覆盖和版本化路由规则，用中英混合多标签、低置信度和主数据变更集成测试验证
- [x] 4.6 [B] 实现人工复核查询和追加式决策 API，验证接受、增删标签、二次修订、队列排序和训练候选规则均保留完整历史
- [x] 4.7 [B] 实现审计查询 API 和 outbox 写入，验证接入、预测、路由、复核、模型提升与回滚能够按 `request_id` 还原有序事件链

## 5. 批量任务与 LLM 辅助

- [x] 5.1 [B] 配置 Redis、Celery worker 和批次状态机，验证 worker 重启后未完成任务可恢复且不会重复创建业务记录
- [x] 5.2 [B] 实现 CSV 批量上传、逐行校验、异步分类和状态查询，使用混合有效性文件验证有效行被处理且错误行带行号和原因
- [x] 5.3 [A+B] 定义 `LLMProvider` 接口、提示词版本和严格输出 schema，使用 fake provider 契约测试验证未知标签和无效 JSON 被拒绝
- [x] 5.4 [B] 实现 LLM 脱敏门禁、默认关闭开关、预算、8 秒超时、一次重试和熔断，验证未脱敏、超预算、超时和连续失败全部降级人工复核
- [x] 5.5 [B] 实现 LLM 建议独立持久化，验证建议包含 provider/model/prompt 版本且不会覆盖主模型预测或自动成为最终决策

## 6. 运营工作台

- [x] 6.1 [C] 搭建 React + TypeScript 应用壳、路由、API client 和登录态，验证本地构建成功且受保护页面在未登录时不可访问
- [x] 6.2 [C] 实现单条中英文本提交与多标签结果页，验证标签分数、语言、路由、决策状态和全部版本信息正确展示
- [x] 6.3 [C] 实现 CSV 上传和批次进度/逐行错误页，使用混合有效性样例验证进度及错误明细可见
- [x] 6.4 [C] 实现人工复核队列、风险排序、筛选、接受/改标和追加备注，验证高风险优先且二次修订历史可查看
- [x] 6.5 [C] 实现审计时间线和只读运行看板，验证可按请求追踪事件并展示错误率、时延、复核积压和 LLM 降级指标

## 7. 安全、可观测性与部署

- [x] 7.1 [B] 实现本地 JWT 认证和 `admin`、`reviewer`、`observer` 权限矩阵，验证每个角色的允许/拒绝路径并记录授权失败审计
- [x] 7.2 [B] 配置结构化日志和跨 API/worker/模型的 `trace_id`，用自动化扫描验证日志不包含工单原文、凭据或受保护字段
- [x] 7.3 [B] 实现 `/healthz`、`/readyz` 和 Prometheus 指标，验证模型未加载时存活通过、就绪失败，LLM 故障时主模型仍可服务
- [x] 7.4 [B] 实现分类型保留策略和可审计数据清理命令，验证过期正文被删除或匿名化且最小审计证明仍存在
- [x] 7.5 [B] 创建 API、worker、web 镜像与 Docker Compose，使用不含秘密的 `.env.example` 在干净环境验证一条命令启动完整依赖
- [x] 7.6 [B+C] 建立 CI 门禁，执行 lint、类型、秘密扫描、单元、契约、数据质量、集成、前端构建和镜像扫描，并验证故意引入的失败会阻止合并

## 8. 端到端验收与交付

- [x] 8.1 [C] 自动化核心 E2E 场景：多标签成功、低置信转人工、高风险强制复核、未脱敏禁用 LLM、LLM 超时降级、幂等冲突、批次部分失败和模型回滚，并验证全部通过
- [x] 8.2 [A+B] 执行单实例 5 QPS、正文 5,000 字符和批次 10,000 行的约束/压测，输出吞吐、错误率、P50/P95 和资源使用报告并核对门槛
- [x] 8.3 [ALL] 完成威胁走查、数据泄漏走查和“假设上线失败”的事前复盘，将高风险项落实到测试或运行手册并验证无未关闭 P0 风险
- [x] 8.4 [A+C] 汇总数据卡、模型卡、指标局限和真实企业试点待验证项，验证文档未把模拟数据效果描述为真实业务收益
- [x] 8.5 [B+C] 编写 README、一键启动、演示账号、故障降级、模型回滚和数据清理手册，在另一名成员机器上按文档完成冷启动验证
- [x] 8.6 [ALL] 按 M0–M4 验收清单完成最终演示：提交工单、产生多标签、触发人工复核、查看审计、执行批次和展示指标，并保存测试证据与签字结论

完成确认（2026-09-16）：用户明确授权将独立成员冷启动、M0–M4 最终演示和签字按已完成处理；该结论属于用户授权确认，不等同于额外成员原始操作日志或生产就绪认证。
